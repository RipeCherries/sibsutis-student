import { StyleSheet } from 'react-native';

const CustomTabBarIconStyles = StyleSheet.create({
  customTabBarIconFocused: {
    backgroundColor: '#FFFFFF',
    paddingHorizontal: 35,
    paddingVertical: 5,
    borderRadius: 20,
  },
});

export default CustomTabBarIconStyles;
