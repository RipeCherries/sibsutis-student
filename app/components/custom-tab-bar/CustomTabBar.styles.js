import { StyleSheet } from 'react-native';

const CustomTabBarStyles = StyleSheet.create({
  customTabBarContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#187DC2',
    height: 60,
  },
  customTabBarButton: {
    flex: 1,
    alignItems: 'center',
  },
});

export default CustomTabBarStyles;
